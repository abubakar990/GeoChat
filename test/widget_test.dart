import 'package:flutter_test/flutter_test.dart';

// Widget tests are disabled while the app requires Supabase initialization.
// Run integration tests against a real device / emulator with valid API keys.
void main() {
  testWidgets('Placeholder — real tests require Supabase init', (tester) async {
    expect(true, isTrue);
  });
}
